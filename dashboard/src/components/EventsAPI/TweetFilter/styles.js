export const styles = theme => ({
  card: {
    overflow: "auto"
  },
  dateContainer: {
    display: "flex",
    justifyContent: "space-evenly",
  },
  languageContainer: {
    margin: theme.spacing.unit,
    minWidth: 200,
  }
});